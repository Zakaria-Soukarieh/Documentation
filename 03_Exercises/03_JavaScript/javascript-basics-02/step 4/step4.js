
function confirm_reset() {
    return confirm("Are you sure you want to reset all text?");

    var getValue= document.getElementById("name");
          getValue.value = "";
      
      var getValue= document.getElementById("city");
          getValue.value = "";
      
      var getValue= document.getElementById("surname");

          getValue.value = "";
      }

